<h1>{{title}}</h1>

<ul>
  <li>
    <a href="biz-simple?section=">
      <span>🌎</span>
      전체
    </a>
  </li>
  {{#each sections}}
    <li>
      <a href="biz-simple?section={{section_id}}">
        {{! 섹션 아이디가 들어감 }}
        <span>{{icon}}</span>
        {{section_name}}
        {{! 한식,분식,중식 등 sql에서 섹션 네임에 해당 }}
      </a>
    </li>
  {{/each}}
</ul>